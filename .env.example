# LangSmith Extractor Configuration
# Copy this file to .env and fill in your actual values

# LangSmith API Configuration
# Required: Get your API key from https://smith.langchain.com
LANGSMITH_API_KEY=your-langsmith-api-key-here

# Optional: Override the default API URL if using a self-hosted instance
# LANGSMITH_API_URL=https://api.smith.langchain.com

# Output Configuration
# Optional: Directory where trace data will be saved (default: ./data)
# OUTPUT_DIR=./data

# Logging Configuration
# Optional: Set logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
# LOG_LEVEL=INFO

# ================================================================================
# Google Drive Configuration (for archive functionality)
# ================================================================================

# Required for archive features: Google Drive folder URL for storing archived traces
# Can be a personal Drive folder or a Shared Drive folder
GOOGLE_DRIVE_FOLDER_URL=https://drive.google.com/drive/folders/YOUR-FOLDER-ID

# Authentication type: 'oauth2' (default) or 'service_account'
GOOGLE_DRIVE_AUTH_TYPE=oauth2

# --------------------------------------------------------------------------------
# For OAuth2 authentication (default)
# --------------------------------------------------------------------------------
# Required: OAuth2 credentials from Google Cloud Console
# 1. Go to https://console.cloud.google.com
# 2. Select/create a project
# 3. Enable Google Drive API
# 4. Go to APIs & Services > Credentials
# 5. Create OAuth 2.0 Client ID (Desktop application)
# 6. Copy the client ID and secret below

GOOGLE_OAUTH_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_OAUTH_CLIENT_SECRET=your-client-secret
GOOGLE_OAUTH_PROJECT_ID=your-project-id  # Optional but recommended

# --------------------------------------------------------------------------------
# For Service Account authentication (alternative)
# --------------------------------------------------------------------------------
# Only needed if GOOGLE_DRIVE_AUTH_TYPE=service_account
# Download the service account JSON key file and specify its path
# GOOGLE_DRIVE_SERVICE_ACCOUNT_PATH=./service-account.json

# Note: For OAuth2, after initial authentication, a token.json file will be created
# to cache your authentication. Keep this file secure and don't commit it to git.